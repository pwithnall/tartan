Support automatically adding the ‘null’ compiler attribute to parameters and return types as a complement to the nonnull attribute (i.e. everything should either be null or nonnull).

This depends on the null attribute actually being implemented first: http://llvm.org/bugs/show_bug.cgi?id=6786.
